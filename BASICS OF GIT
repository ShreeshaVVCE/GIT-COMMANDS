git config --list
git config --global user.name "ShreeshaVVCE"
git config --global user.email "vvce23cse0189@vvce.ac.in"
ls   
git init
ls -a

git add .
git status
git remote add origin "url"

{
  git add .
  git commit -m "hi this is git"
  git branch -m main
  git push -u origin main
}
